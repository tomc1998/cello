
(fn foo __slice_u8 (argc u64 argv (ptr (ptr u8)))
  (let a u64 3)
  (let b u64 4)
  (let res u64 (+ a b))
  (let res2 u64 (if (> b a) res 0))
  (let res3 u64 (+ res2 3))
  (let res4 u64 (if (> res3 res2)
                  (if (> res3 res) res3 res)
                  (if (> res2 res) res2 res)))
  (let test_str __slice_u8 "Hello")
  test_str)

(fn add u64 (a u64 b u64)
  (+ a b))